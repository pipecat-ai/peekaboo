ANTHROPIC_API_KEY=your_anthropic_key_here

# Only for `uv run src/bot.py -t daily`, the remote demo. The Mac app needs
# nothing else: speech recognition (Moonshine) and synthesis (Kokoro) run
# locally.
DAILY_API_KEY=your_daily_api_key_here
DAILY_SAMPLE_ROOM_URL=your_daily_sample_room_url_here
